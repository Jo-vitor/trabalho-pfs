import React, { useState } from "react";

const InserirCarro = ({ }) => {

    /*
    return (
        <div className="container mt-5">
            <h1>Inserir Carro</h1>
            <div className="mb-3">
                <label htmlFor="image" className="form-label">Link da Foto do Carro</label>
                <input
                    type="text"
                    className="form-control"
                    id="image"
                    name="image"
                    value={novoCarro.image}
                    onChange={handleChange}
                />
            </div>
            <div className="mb-3">
                <label htmlFor="title" className="form-label">Modelo</label>
                <input
                    type="text"
                    className="form-control"
                    id="title"
                    name="title"
                    value={novoCarro.title}
                    onChange={handleChange}
                />
            </div>
            <div className="mb-3">
                <label htmlFor="price" className="form-label">Valor da Diária</label>
                <input
                    type="text"
                    className="form-control"
                    id="price"
                    name="price"
                    value={novoCarro.price}
                    onChange={handleChange}
                />
            </div>
            <div className="mb-3">
                <label htmlFor="description" className="form-label">Descrição</label>
                <input
                    type="text"
                    className="form-control"
                    id="description"
                    name="description"
                    value={novoCarro.description}
                    onChange={handleChange}
                />
            </div>
            <button
                type="button"
                className="btn btn-success"
                onClick={handleSubmit}
            >
                Criar
            </button>
        </div>
    );
    */
};

export default InserirCarro;